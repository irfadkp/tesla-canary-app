apiVersion: v1
kind: Service
metadata:
  name: tesla-frontend
  labels:
    app: tesla-frontend
    component: ui
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: tesla-frontend
