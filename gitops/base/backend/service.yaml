apiVersion: v1
kind: Service
metadata:
  name: tesla-backend
  labels:
    app: tesla-backend
    component: api
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: tesla-backend
---
apiVersion: v1
kind: Service
metadata:
  name: tesla-backend-stable
  labels:
    app: tesla-backend
    component: api
    service-type: stable
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: tesla-backend
---
apiVersion: v1
kind: Service
metadata:
  name: tesla-backend-canary
  labels:
    app: tesla-backend
    component: api
    service-type: canary
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: tesla-backend